import Config from 'react-native-config';

export const { BASE_URL } = Config;

export const API = {
  POST: {
    posts: '/posts',
  },
  USER: {
    users: '/users',
  },
};

export const NAVIGATION = {
  HOME: {
    HomeMain: 'HomeMain',
  },
};
